# متجر البقالة الإلكتروني | Fresh Grocery Store

موقع متجر بقالة إلكتروني ثابت (Static) مبني باستخدام HTML, CSS, و JavaScript النقي.

## 📋 المحتويات

```
grocery-store/
├── index.html          # الصفحة الرئيسية
├── admin.html          # لوحة التحكم
├── css/
│   ├── style.css      # التصميم الرئيسي
│   └── admin.css      # تصميم لوحة التحكم
├── js/
│   ├── data.js        # إدارة البيانات (localStorage)
│   ├── router.js      # نظام التوجيه
│   ├── app.js         # المنطق الرئيسي
│   └── admin.js       # منطق لوحة التحكم
└── images/            # مجلد الصور
```

## 🚀 كيفية الاستخدام

### 1. تشغيل الموقع

ببساطة افتح ملف `index.html` في المتصفح:
- انقر نقراً مزدوجاً على `index.html`
- أو استخدم Live Server في VS Code

### 2. الوصول إلى لوحة التحكم

افتح `admin.html` في المتصفح:

**بيانات الدخول الافتراضية:**
- اسم المستخدم: `admin`
- كلمة المرور: `admin123`

## ✨ الميزات الرئيسية

### للمستخدمين (الموقع العام)

✅ **الصفحة الرئيسية**
- عرض تقديمي جذاب
- الأقسام المميزة
- المنتجات المميزة

✅ **صفحة الأقسام**
- عرض جميع أقسام المنتجات
- البحث السريع

✅ **صفحة المنتجات**
- كتالوج كامل للمنتجات
- فلاتر متقدمة حسب القسم
- البحث بالاسم/الباركود
- ترتيب النتائج
- Pagination

✅ **صفحة تفاصيل المنتج**
- معلومات كاملة
- معرض صور
- اختيار الكمية
- زر واتساب مباشر

✅ **صفحة التواصل**
- معلومات الاتصال
- خريطة Google Maps
- أزرار تواصل سريعة

### للمدراء (لوحة التحكم)

✅ **إدارة المنتجات**
- إضافة/تعديل/حذف المنتجات
- رفع الصور
- تحديد الأسعار والوحدات
- تفعيل/إلغاء تفعيل

✅ **إدارة الأقسام**
- إنشاء أقسام جديدة
- تعديل الأقسام الموجودة
- رفع صور الأقسام

✅ **إدارة الماركات**
- إضافة ماركات المنتجات
- ربط المنتجات بالماركات

✅ **إدارة البانرات**
- إضافة إعلانات وعروض
- تحديد موضع البانر
- جدولة العروض

✅ **الإعدادات**
- بيانات المتجر
- معلومات الاتصال
- ساعات العمل
- خريطة Google Maps

## 📱 الميزات التقنية

### تصميم متجاوب (Responsive)
- يعمل على جميع الأجهزة
- تصميم Mobile-First
- قوائم قابلة للطي

### الأداء
- تحميل سريع
- لا يوجد استدعاءات خارجية (ما عدا الخطوط والأيقونات)
- استخدام localStorage للتخزين المحلي

### تجربة المستخدم (UX)
- تأثيرات حركية سلسة
- إشعارات Toast
- حالات التحميل
- حالات فارغة (Empty States)

### SEO
- عناوين دلالية (Semantic HTML)
- Meta tags
- Alt text للصور

## 🎨 التخصيص

### الألوان

يمكنك تغيير الألوان من ملف `css/style.css`:

```css
:root {
    --primary: #2ecc71;      /* اللون الأساسي */
    --accent: #ff6b35;       /* لون التمييز */
    --dark: #1a1a2e;         /* اللون الداكن */
    /* ... */
}
```

### الخطوط

الخطوط المستخدمة:
- **العربية**: Tajawal (Google Fonts)
- **الإنجليزية**: Poppins (Google Fonts)

يمكنك تغييرها من `index.html` و `admin.html`.

### الصور

لإضافة صور حقيقية:
1. ضع الصور في مجلد `images/`
2. استخدم المسارات النسبية: `images/products/product1.jpg`
3. أو استخدم روابط خارجية
4. أو استخدم Base64 للصور الصغيرة

## 🔧 التطوير والتحسين

### إضافة بيانات جديدة

جميع البيانات مخزنة في `localStorage`. لإضافة بيانات أولية جديدة، عدّل دالة `seedData()` في ملف `js/data.js`.

### إضافة صفحات جديدة

1. سجل route جديد في `js/app.js`:
```javascript
Router.register('page-name', () => this.renderPageName());
```

2. أنشئ دالة العرض:
```javascript
renderPageName() {
    const app = document.getElementById('app');
    app.innerHTML = `...`;
}
```

### إضافة حقول جديدة للمنتج

1. عدّل نموذج البيانات في `data.js`
2. عدّل النموذج في `admin.js`
3. عدّل واجهة العرض في `app.js`

## 📊 البيانات المخزنة

يستخدم الموقع `localStorage` لتخزين:
- المنتجات
- الأقسام
- الماركات
- البانرات
- الإعدادات
- بيانات المدير

### حذف جميع البيانات

افتح Console في المتصفح واكتب:
```javascript
localStorage.clear();
```
ثم أعد تحميل الصفحة.

## 🔒 الأمان

⚠️ **ملاحظة مهمة**: هذا موقع ثابت (Static) للاستخدام التعليمي أو للعروض التوضيحية. 

**لا ينصح باستخدامه في بيئة إنتاجية** بدون:
- Backend حقيقي
- قاعدة بيانات
- نظام مصادقة آمن
- HTTPS
- حماية ضد CSRF/XSS

## 🌐 المتصفحات المدعومة

- ✅ Chrome (أحدث إصدار)
- ✅ Firefox (أحدث إصدار)
- ✅ Safari (أحدث إصدار)
- ✅ Edge (أحدث إصدار)
- ⚠️ IE11 (غير مدعوم)

## 📝 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الحر.

## ⚙️ PowerShell Scripts

The following scripts are available in the project root for common development tasks:

### `run.ps1` — Run the API

Starts the backend API server.

```powershell
.\run
```

The API will be available at `http://localhost:5050` with Scalar API docs at `/scalar/v1`.

---

### `db.ps1` — Database Migrations

Creates and applies EF Core migrations.

```powershell
# Create a new migration and apply it
.\db -Name "MigrationName"

# Apply pending migrations only (no new migration)
.\db
```

| Parameter | Alias | Description |
|-----------|-------|-------------|
| `-Name` | `-MigrationName` | Name of the new migration to create. If omitted, only pending migrations are applied. |

---

### `test.ps1` — Run Tests

Builds the solution and runs test projects.

```powershell
# Run all test projects
.\test

# Run a specific test project by keyword
.\test domain               # or short name like dom
.\test application      # or short name like app
.\test infrastructure   # or short name like inf
.\test api              
.\test cqrs
```

| Keyword | Test Project |
|---------|-------------|
| `domain` | GroceryStore.Domain.Tests |
| `application` | GroceryStore.Application.Tests |
| `infrastructure` | GroceryStore.Infrastructure.Tests |
| `api` | GroceryStore.Api.Tests |
| `cqrs` | CQRS.Tests |

## 👨‍💻 المطور

**Juan Khanjar**
- Email: jan4ma@gmail.com
  
**Majed Girover**
- Email: girover.mhf@gmail.com

---

## 🚀 البدء السريع

1. **افتح الموقع**: انقر مرتين على `index.html`
2. **تصفح المنتجات**: استكشف الأقسام والمنتجات
3. **جرب البحث**: استخدم شريط البحث العلوي
4. **افتح لوحة التحكم**: افتح `admin.html` واستخدم `admin/admin123`
5. **أضف منتجات**: جرب إضافة منتجات جديدة من لوحة التحكم
6. **خصص الإعدادات**: غيّر معلومات المتجر من صفحة الإعدادات

## 📞 التواصل عبر WhatsApp

عند النقر على زر "واتساب":
- يتم فتح WhatsApp تلقائياً
- يتم ملء الرسالة باسم المنتج والكمية والسعر
- جاهز للإرسال مباشرة

مثال على الرسالة:
```
مرحباً، أريد طلب:
طماطم طازجة
الكمية: 2 kg
السعر الإجمالي: 5.00 IQD
```

## 🎯 حالات الاستخدام

- ✅ متجر بقالة محلي
- ✅ سوبر ماركت صغير
- ✅ متجر خضار وفواكه
- ✅ متجر منتجات عضوية
- ✅ عرض كتالوج منتجات
- ✅ مشروع تعليمي

---

**استمتع باستخدام الموقع! 🛒🎉**
