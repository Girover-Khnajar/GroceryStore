@model CategoryFormViewModel
@{
    Layout = "_AdminLayout";
    var isEdit = Model.Id.HasValue;
    ViewData["Title"] = isEdit ? "Edit Category" : "Add Category";
}

<div class="form-card" style="max-width:640px;">
    <form asp-action="@(isEdit ? "CategoryEdit" : "CategoryCreate")"
          asp-route-id="@Model.Id"
          method="post">
        @Html.AntiForgeryToken()
        <div asp-validation-summary="ModelOnly" style="color:var(--danger);margin-bottom:1rem;"></div>

        <div class="form-row">
            <div class="form-group">
                <label asp-for="Name" class="form-label"></label>
                <input asp-for="Name" class="form-control" placeholder="e.g. Fresh Vegetables" />
                <span asp-validation-for="Name" class="validation-message"></span>
            </div>
            <div class="form-group">
                <label asp-for="Slug" class="form-label"></label>
                <input asp-for="Slug" class="form-control" placeholder="fresh-vegetables" />
                <span asp-validation-for="Slug" class="validation-message"></span>
            </div>
        </div>

        <div class="form-group">
            <label asp-for="ParentCategoryId" class="form-label">Parent Category</label>
            <select asp-for="ParentCategoryId" class="form-control">
                <option value="">— None (root) —</option>
                @foreach (var p in Model.AvailableParents.Where(c => c.Id != Model.Id))
                {
                    <option value="@p.Id">@p.Name</option>
                }
            </select>
        </div>

        <div class="form-group">
            <label asp-for="Description" class="form-label"></label>
            <textarea asp-for="Description" class="form-control" rows="3"></textarea>
        </div>

        <div class="form-row">
            <div class="form-group">
                <label asp-for="ImageUrl" class="form-label">Image URL</label>
                <input asp-for="ImageUrl" class="form-control" placeholder="https://..." />
            </div>
            <div class="form-group">
                <label asp-for="IconName" class="form-label">Icon (FontAwesome)</label>
                <input asp-for="IconName" class="form-control" placeholder="fas fa-carrot" />
            </div>
        </div>

        <div class="form-group" style="max-width:200px;">
            <label asp-for="SortOrder" class="form-label"></label>
            <input asp-for="SortOrder" type="number" class="form-control" />
        </div>

        <hr style="border:none;border-top:1px solid var(--border);margin:1.5rem 0;" />
        <h4 style="font-size:1rem;font-weight:700;margin-bottom:1rem;">SEO</h4>

        <div class="form-group">
            <label asp-for="SeoMetaTitle" class="form-label">Meta Title</label>
            <input asp-for="SeoMetaTitle" class="form-control" />
        </div>
        <div class="form-group">
            <label asp-for="SeoMetaDescription" class="form-label">Meta Description</label>
            <textarea asp-for="SeoMetaDescription" class="form-control" rows="2"></textarea>
        </div>

        <div style="display:flex;gap:1rem;justify-content:flex-end;margin-top:1.5rem;">
            <a asp-action="Categories" class="btn-secondary"><i class="fas fa-times"></i> Cancel</a>
            <button type="submit" class="btn-submit"><i class="fas fa-save"></i> Save</button>
        </div>
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
